/*
path: autocomplete.json
completePath: Fields/autocomplete.json
unique_id: zRbGEoge
*/
{
  "label": "Autocomplete",
  "value": "Autocomplete",
  "dataType": "mongoose.Schema.Types.ObjectId",
  "initialValue": "",
  "options": {
    "extra": [
      {
        "display": "Relationship Type",
        "value": "relationshipType",
        "type": "select",
        "options": "[{ label: '1 to many', value: '1:m'}, { label: 'many to 1', value: 'm:1'}]"
      },
      {
        "display": "From",
        "value": "reference",
        "type": "select",
        "options": "aptugo.tableUtils.getAllFields().map(field => { return { label: field[1], value: field[0] } })"
      },
      {
        "display": "ReferencedKey",
        "value": "referencekey",
        "type": "select",
        "options": "[{ label: 'Standard Key', value: '_id' }, ...aptugo.tableUtils.getAllFields().map(field => { return { label: field[1], value: field[0] } })]"
      },
      {
        "display": "Display",
        "value": "displaytype",
        "type": "select",
        "options": [
          {
            "label": "Standard",
            "value": "standard"
          },
          {
            "label": "Chips / Tags",
            "value": "chips"
          }
        ]
      }
    ]
  }
}